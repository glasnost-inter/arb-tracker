pada page /admin

koneksi ke database mysql

tombol backup database. 

ketika tombol di klik maka akan melakukan backup database dan menyimpan file backup ke folder ./backups

tombol restore database. 

ketika tombol di klik maka akan melakukan restore database dari file backup yang ada di folder ./backups
