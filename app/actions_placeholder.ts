// Append this to existing actions.ts or creating new if needed.
// Since I can't append easily with write_to_file, I will read actions.ts logic first (I have viewed it before but it's large).
// I will use `replace_file_content` to add the new action.
